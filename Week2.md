# 데이터분석 2주차 정규과제

📌데이터분석 정규과제는 매주 정해진 분량의 『*혼자 공부하는 데이터 분석 with 파이썬*』 을 읽고 학습하는 것입니다. 이번 주는 아래의 **DataAnalysis_2nd_TIL**에 나열된 분량을 읽고 공부하시면 됩니다.

아래의 문제를 풀어보며 학습 내용을 점검하세요. 문제를 해결하는 과정에서 개념을 스스로 정리하고, 필요한 경우 제시된 강의를 참고하여 보완하는 것이 좋습니다.

<!-- 강의 링크는 아래와 같습니다.
https://www.youtube.com/watch?v=s_-VvTLb3gs&list=PLVsNizTWUw7FGzSRCkQrPEEe-ljVXgS7k&index=4
https://www.youtube.com/watch?v=Il6L8OtNFpc&list=PLVsNizTWUw7FGzSRCkQrPEEe-ljVXgS7k&index=5
-->


## DataAnalysis_2nd_TIL

### 2장 데이터 수집하기
#### 01. API 사용하기
#### 02. 웹 스크래핑 사용하기


## Study Schedule

| 주차  | 공부 범위     | 완료 여부 |
| ----- | ------------- | --------- |
| 1주차 | p.24~81    | ✅         |
| 2주차 | p.84~151   | ✅         |
| 3주차 | p.154~219  | 🍽️         |
| 4주차 | p.222~279 | 🍽️         |
| 5주차 | p.282~325 | 🍽️         |
| 6주차 | p.328~379 | 🍽️         |
| 7주차 | p.382~430 | 🍽️         |

<br>

<!-- 여기까진 그대로 둬 주세요-->


# 1️⃣ 개념 정리 

## 01. API 사용하기

API는 프로그램 간 데이터를 주고받기 위해 정해둔 통신 규칙을 말한다.
데이터베이스의 보안이나 물리적 제한 때문에 직접 접근할 수 없는 경우가 많은데, 이때 인증된 URL(API 주소)을 통해 요청을 보내면 원하는 데이터를 안전하게 가져올 수 있다.


**<새롭게 배운 내용>**

**1.** 데이터 형식: 웹 API는 주로 HTTP 프로토콜을 사용하는데 전달하는 데이터가 너무 복잡한 HTML 구조이면 프로그램에 버그가 생길 위험이 크다.
               따라서 구조가 직관적이고 텍스트로 이루어져 사람이 읽기 편한 JSON이나 XML 방식을 주로 사용한다.

**2.** 파이썬에서 API를 호출할 때는 널리 사용되는 requests 패키지의 requests.get() 함수를 사용하여 간편하게 URL 데이터를 요청하고 전달받을 수 있음.

**3.** 특히 JSON은 파이썬의 딕셔너리 자료형과 구조가 거의 같아서 데이터 분석에 매우 유리하다.
   파이썬 내장 json 패키지의 loads() 함수를 쓰면 JSON 문자열을 파이썬 객체로 바꿀 수 있고, 판다스의 read_json() 함수를 이용하면 분석하기 편한 데이터프레임 표 형태로 바로 변환할 수 있다.

   
## 02.웹 스크래핑 사용하기

웹 스크래핑은 파이썬과 같은 프로그램을 이용해 웹사이트의 페이지들을 옮겨 다니면서 분석에 필요한 데이터만 자동으로 골라내 추출하는 기술이다.
API가 공식적인 데이터 창구 역할을 한다면 웹 스크래핑은 화면에 보이는 웹 페이지 전체(HTML)를 가져온 뒤에 필요한 것만 걸러내는 작업이라고 볼 수 있다.

- **BeautifulSoup의 역할**
  : requests.get() 함수로 가져온 웹 페이지의 HTML 소스코드는 많은 태그와 텍스트가 섞여 있어서 필요한 특정 링크나 텍스트만 정확하게 찾아내기가 어렵다.
  BeautifulSoup 패키지를 사용하면 복잡한 HTML 문서의 구조를 파악하고 태그들을 체계적으로 탐색하여 원하는 데이터를 쉽게 추출할 수 있다는 점에서 그 역할이 부각되고 있다.
- **파서의 중요성**
  : 객체를 만들 때 BeautifulSoup(가져온데이터, 'html.parser')처럼 파서를 반드시 지정해야 한다. 파서는 단순한 텍스트 덩어리를 프로그램이 이해할 수 있는 데이터 구조로 쪼개고 분석해 주는 역할을 한다.
- **스크래핑 시 주의할 점**
  : 자동화된 프로그램은 단시간에 수만 번의 접속 요청을 보낼 수 있기에 웹 서버를 마비시킬 위험이 있다. 따라서 크롤링 전에는 반드시 웹사이트의 robots.txt 파일 주소를 확인하여 검색 로봇이나 프로그램의 접근이 허용된 페이지인지 먼저 점검하는 것이 중요하다.
  -> 실제로 colab 상에서 공공데이터 크롤링을 시도하다 계속 접근 차단으로 실패했던 경험이 있어 사전 점검의 중요성을 파악한 적이 있다.


# 2️⃣ 수행 인증

<!-- 교재에서 안내된 과정을 직접 실행해본 뒤, 진행 결과가 보이도록 4~6장의 스크린샷을 캡처하여 아래에 첨부해주세요.-->

<img width="2658" height="1198" alt="image" src="https://github.com/user-attachments/assets/bde326c4-05a9-4275-bf4d-490eb09245f1" />

<img width="2666" height="1198" alt="image" src="https://github.com/user-attachments/assets/9b467476-c447-4f61-ae5d-25c4670f8d27" />


**웹 스크래핑**

<img width="2678" height="1162" alt="image" src="https://github.com/user-attachments/assets/5664c028-bd9f-49ce-84e3-53ffc08d94a2" />

<img width="1480" height="1118" alt="image" src="https://github.com/user-attachments/assets/ba6e8acc-dbce-472a-8fbf-9e72d74b5afe" />
<img width="900" height="514" alt="image" src="https://github.com/user-attachments/assets/65706a56-525e-46a3-8aec-5145d0515232" />




<br>
<br>

# 3️⃣ 확인 문제

## 문제 1.

> **🧚Q. 다음 중 BeautifulSoup 외에 웹 스크래핑에 사용할 수 있는 파이썬 패키지로 가장 적절한 것은 무엇인가요?**

```
1️⃣ NumPy  
2️⃣ Scrapy  
3️⃣ Matplotlib  
4️⃣ Scikit-learn  
```

```
답: 2번 Scrapy

설명: 교재의 내용에서 볼 수 있듯이 최근 Scrapy와 같은 패키지의 인기가 높은데, 이는 데이터를 요청하여 가져오는 requests 패키지와 가져온 HTML에서 데이터를 추출하는 BeautifulSoup의 기능을 하나로 합쳐 놓은 것과 같은 역할을 하기 때문이다. 단순한 요소 추출을 넘어 대규모 웹 사이트를 체계적이고 빠르게 긁어올 때 필수적인 도구라고 볼 수 있다.
```



### 🎉 수고하셨습니다.
